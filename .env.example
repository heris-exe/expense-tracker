# Supabase (required for cloud sync across devices)
# Get these from: https://app.supabase.com → your project → Settings → API
VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# Production only: your live app URL (e.g. https://your-app.vercel.app).
# Set this so Google sign-in redirects back to production, not localhost.
# Optional; if unset, the app uses the current page origin.
# VITE_APP_URL=https://your-app.vercel.app
